# Codebook — Resources

*11,877 rows across 3 file(s). Generated 2026-08-07.*

Variables marked **internal** are retained for auditing and are not included in published extracts.

| Variable | Type | Units / format | Filled | Description |
|---|---|---|---:|---|
| `resource_revenue_event_id` | text | code | 100% | Identifier for one payment or receipt event in the resource ledger. |
| `recipient_entity_id` | text | code | 6% | The entity that received the payment. Blank where the source names no entity. |
| `recipient_entity_name` | text | text | 9% | Name. |
| `beneficiary_entity_id` | text | code | 5% | The entity for whose benefit the money is held or paid, which is not always the recipient - a federal trust account can receive money owed to an individual allottee. Blank where the source names none. |
| `beneficiary_entity_name` | text | text | 5% | Name. |
| `beneficiary_note` | text | text | 100% | Who the money is actually for, where that differs from what the dataset name suggests. On federal rows it records that the figure is REVENUE FROM NATIVE AMERICAN LANDS and NOT payments to tribal governments: the federal land class mixes tribal mineral interests with individual Indian (allottee) interests, and no tribe can be named from such a row. |
| `payer_entity_id` | text | code | 9% | The paying party. A `PAYER-` prefix marks a non-Native counterparty such as a state or a federal bureau; those are labels, not Cedar Press entity identifiers, and never resolve to a Native entity. |
| `payer_entity_name` | text | text | 11% | Name. |
| `operator_entity_id` | empty | code | 0% | The company operating the asset. An operator works the resource and owns no interest in it; operating is never ownership. |
| `operator_entity_name` | empty | text | 0% | Name. |
| `related_asset_ids` | empty | delimited identifiers | 0% | Resource assets this event relates to, delimited. Blank where the source publishes no asset-level detail. |
| `source_system` | text |  | 100% | The publishing system the record came from, named at the level a reader needs to find it again - the agency's system and the specific series within it, e.g. `ONRR_NRRD_monthly_revenue` or `ANCSA_7i_7j_annual_reports`. |
| `source_record_id` | text | code | 100% | Identifier. |
| `revenue_type` | text | categorical | 100% | The kind of payment: `royalty`, `bonus`, `rent`, `lease_payment`, `surface_damage_payment`, `severance_tax_share`, `production_tax_share`, `trust_disbursement`, `direct_pay`, `fund_deposit`, `grant_from_resource_fund`. `other_reported_revenue` is deliberately outside that list: it carries source categories that group several unlike things together, and splitting them would invent a distinction the source does not make. |
| `resource_type` | text | categorical | 100% | Coarse commodity family - `oil_and_gas`, `coal`, `hardrock`, `geothermal`, `sand_and_gravel`, `other_mineral`, `mixed`, `not_stated`. The source's own wording is preserved in `commodity` and `mineral_lease_type`. |
| `commodity` | text | text | 99% | The commodity as the source names it, preserved verbatim rather than recoded, so a source's own categories stay recoverable. |
| `product` | text | text | 71% | The specific product sold, as the source names it - unprocessed gas, residue gas, condensate and so on. Blank where the source records a commodity but no product, which is normal before production begins. |
| `mineral_lease_type` | text | text | 88% | The lease type as the source names it, preserved verbatim. |
| `period_type` | text | categorical | 100% | The kind of period the amount covers - `month`, `calendar_year`, `federal_fiscal_year`, `state_fiscal_year`, `quarter`. A federal fiscal year is not a calendar year and the two must not be plotted on one axis without saying so. |
| `period_start` | text | YYYY-MM-DD | 95% | First day of the period the amount covers. |
| `period_end` | text | YYYY-MM-DD | 95% | Last day of the period the amount covers. |
| `payment_date` | text | YYYY-MM-DD | 5% | Date. |
| `amount_usd` | numeric | USD, nominal | 100% | Amount. |
| `amount_usd_real2025` | numeric | USD, constant 2025 | 97% | Amount restated in constant 2025 dollars. BLANK where the period falls in a year with no published annual price index; a blank is not a zero and must not be filled with the nominal value. |
| `deflator_factor_2025` | numeric | ratio | 97% | The factor multiplying nominal dollars to reach constant 2025 dollars. Base years must never be mixed across a sum. |
| `inflation_base_year` | integer | YYYY | 97% | Year. |
| `measurement_status` | text | categorical | 100% | WHAT KIND OF FACT THE AMOUNT IS, and the column to read before summing anything. `actual_payment` - money left the payer. `reported_revenue` - a collector reports having received it, which is not the same as anyone being paid. `statutory_allocation` - a statute directs a share; it is not evidence the share was paid. `budgeted_amount` - proposed. `appropriated_amount` - a legislature appropriated it. A royalty, an appropriation and a state tax distribution are different kinds of fact and must never be added together as though equivalent. |
| `aggregation_level` | text | categorical | 100% | Whether the row describes one named entity (`entity_specific`) or a total covering many (`national_aggregate`, `state_aggregate`). NATIONAL AGGREGATES ALREADY CONTAIN THE ENTITY-SPECIFIC MONEY, so summing across levels double-counts. |
| `land_status` | text | categorical | 100% | Whether the interest sits on `trust` or `fee` land, or `mixed`. `not_stated` MEANS NO SOURCE STATED IT and is the value on most rows. Trust versus fee determines who is owed the money, so it is never inferred from a map: a well inside a reservation boundary is not evidence of tribal mineral ownership. |
| `land_status_basis` *(internal)* | text |  | 100% | One of: `ONRR publishes a Native American land CLASS; it does not state trust vs fee`, `the distribution record does not state trust vs fee acreage`, `not stated by source`, `the annual report does not state land tenure; ANCSA Section 7(i) applies to revenue from the subsurface estate and timber resources conveyed to the corporation under the Act, which is corporate fee land, not federal trust land`, `the 1906 Osage Allotment Act reserved the entire mineral estate of Osage County to the Osage Nation, held in trust by the United States`, `the enabling statute conditions the deposit on interests held in trust by the United States for the tribe and its members`, `MMS published an American Indian lands aggregate; it does not state trust vs fee` |
| `allocation_formula` | text | text | 10% | The revenue-sharing split a statute or agreement sets for this period, as the governing authority states it. BLANK MEANS THE FORMULA FOR THAT PERIOD WAS NOT SOURCED - these splits have been changed by legislation, so a current ratio is not valid for an earlier year and none is carried backwards. |
| `allocation_formula_effective_start` | text | YYYY-MM-DD | 5% | First date the stated allocation formula governs. |
| `allocation_formula_effective_end` | text | YYYY-MM-DD | 2% | Last date the stated allocation formula governs. Blank means still in force as of the fetch date. |
| `allocation_formula_source_url` | text | URL | 10% | Link to the enacted statute or agreement establishing the formula for this period. |
| `amount_sign_meaning` | text | text | 100% | What a negative amount means on this row - a refund, recoupment or prior-period correction, not an error. Negatives are retained and belong in any total. |
| `geography_note` | text | text | 100% | What the geography fields on this row actually contain. On federal Native American rows every geographic field is blank by source design, because extraction and revenue information for Native American land is released only in aggregate. |
| `confidence` | text |  | 100% | One of: `A`, `B` |
| `source_url` | text | URL | 100% | Link to the record's published source. |
| `fetched_date` | text | YYYY-MM-DD | 100% | Date. |
| `built_date` | text | YYYY-MM-DD | 100% | Date. |
| `resource_asset_id` | empty | code | 0% | Cedar Press identifier for a physical or legal resource asset - a well, mine, lease or tract. |
| `asset_type` | empty | categorical | 0% | What kind of asset the row describes - `well`, `mine`, `lease`, `tract`. |
| `asset_name` | empty | text | 0% | Name. |
| `source_asset_id` | empty | code | 0% | The asset's identifier in the system that published it, kept alongside the Cedar identifier so an external system can be joined without rebuilding. |
| `niogems_lease_id` | empty | code | 0% | Lease identifier in BIA's National Indian Oil and Gas Evaluation Management System. EMPTY BY CONSTRUCTION - NIOGEMS is an internal BIA system Cedar Press has no access to. The column exists so the join is a merge rather than a rebuild if access is ever granted. |
| `niogems_tract_id` | empty | code | 0% | Tract identifier in BIA's NIOGEMS. Empty by construction; see `niogems_lease_id`. |
| `niogems_agreement_id` | empty | code | 0% | Agreement identifier in BIA's NIOGEMS. Empty by construction; see `niogems_lease_id`. |
| `niogems_well_id` | empty | code | 0% | Well identifier in BIA's NIOGEMS. Empty by construction; see `niogems_lease_id`. |
| `state` | empty | 2-letter code | 0% | US state or territory. |
| `county` | empty | text | 0% | County as the source names it. Blank where the source publishes no county. |
| `fips_code` | empty | code | 0% | Classification code. |
| `latitude` | empty | decimal degrees | 0% | Latitude of the facility. |
| `longitude` | empty | decimal degrees | 0% | Longitude of the facility. |
| `reservation_name` | empty | text | 0% | The reservation the source associates with this asset. THIS IS LOCATION, NOT OWNERSHIP - read `land_status`. |
| `land_status_source_url` | empty | URL | 0% | Link to the source that states this asset's trust or fee status. Blank means no source stated it. |
| `operator_name` | empty | text | 0% | Name. |
| `status` | empty | text | 0% | The asset's operating status as the source states it. |
| `first_production_date` | empty | YYYY-MM-DD | 0% | Date the asset first produced, as the source states it. |
| `spud_date` | empty | YYYY-MM-DD | 0% | Date drilling began, as the source states it. |
| `geometry_basis` *(internal)* | empty |  | 0% |  |
| `party_link_id` | text | code | 100% | Identifier for one link between a party and an asset or revenue event. Parties are many-to-many: a single well can involve a tribal government, individual allottees, a tribal enterprise, an operator, a lessee and a federal trust account at the same time, so no single owner column exists. |
| `object_type` | text | categorical | 100% | Whether the linked object is a `revenue_event` or an `asset`. One link table serves both so party attachment works the same way for each. |
| `object_id` | text | code | 100% | Identifier. |
| `entity_id` | text | code | 100% | Identifier. |
| `entity_name` | text | text | 100% | Name. |
| `entity_is_native` | integer | 0/1 | 100% | Whether the linked party is a Native entity. A payer or operator party is routinely not. |
| `party_role` | text | categorical | 100% | What the party did on this asset or event - `recipient`, `payer`, `operator`, `lessee`, `allottee`, `trustee`. |
| `relationship` | text | categorical | 100% | Whether the party OWNS the interest (`parent_native_entity`) or merely SERVES it (`serves_native_entities`), or is an outside `counterparty`. Ownership and service are different facts and collapsing them manufactures ownership. |
| `interest_share_pct` | integer | percent | 8% | The party's stated share of the interest. Blank where no source states a share; it is never divided evenly among parties. |
| `basis` | text | text | 100% | The evidence behind this party link - what the source said, or which name the entity resolved from. |

## Value sets

- **`recipient_entity_id`** — `TRBF-MHATAT-00`, `ANRC-CHGCCO-00`, `ANRC-BRBYCO-00`, `ANRC-CKINLT-00`, `ANRC-KONIAG-00`, `ANRC-ALEUTC-00`, `ANRC-DOYONL-00`, `ANRC-NANARC-00`, `ANRC-ARCSLO-00`, `ANRC-SEALSK-00`, `ANRC-CALSTA-00`, `ANRC-BERSTR-00`, `ANRC-AHTNAI-00`
- **`recipient_entity_name`** — `Three Affiliated`, `Holders of Osage headrights (individuals)`, `Osage Mineral Estate`, `Uintah Basin Revitalization Fund`, `Navajo Revitalization Fund`, `Village corporations and at-large shareholders of the region, not individually named in the source`, `The other ANCSA regional corporations, not individually named in the source`, `Chugach Alaska Corporation`, `Bristol Bay Native Corporation`, `Cook Inlet Region, Incorporated`, `Koniag, Incorporated`, `Aleut Corporation`, `Doyon, Limited`, `NANA Regional Corporation, Incorporated`, `Arctic Slope Regional Corporation`, `Sealaska Corporation`, `Calista Corporation`, `Other ANCSA regional corporations, village corporations and at-large shareholders, not individually named in the source`, `Bering Straits Native Corporation`, `State of Oklahoma`, `Ahtna, Incorporated`, `Northwest Arctic Borough`
- **`payer_entity_id`** — `PAYER-STATE-ND`, `PAYER-US-ONRR`, `PAYER-STATE-UT`, `PAYER-US-BIA`, `PAYER-STATE-MT`, `ANRC-NANARC-00`, `ANRC-CKINLT-00`, `ANRC-ALEUTC-00`, `ANRC-ARCSLO-00`, `ANRC-CHGCCO-00`
- **`payer_entity_name`** — `State of North Dakota`, `United States, Office of Natural Resources Revenue`, `State of Utah`, `Other ANCSA regional corporations, not individually named in the source`, `United States, Bureau of Indian Affairs`, `State of Montana`, `NANA Regional Corporation, Incorporated`, `Cook Inlet Region, Incorporated`, `Aleut Corporation`, `Arctic Slope Regional Corporation`, `Mine operator, not named in the quoted passage`, `Osage Mineral Estate`, `Chugach Alaska Corporation`
- **`source_system`** — `ONRR_NRRD_monthly_revenue`, `ND_State_Treasurer_tax_distribution_search`, `ANCSA_7i_7j_annual_reports`, `ONRR_NRRD_fiscal_year_disbursements`, `UT_COBI_fund_financials`, `OMC_headright_payment_history`, `OMC_quarterly_newsletter`, `MT_DOR_county_oil_gas_distribution`, `MMS_MRM_american_indian_revenues`, `MMS_MRM_american_indian_revenues_calendar`
- **`revenue_type`** — `royalty`, `other_reported_revenue`, `rent`, `production_tax_share`, `severance_tax_share`, `trust_disbursement`, `direct_pay`, `ancsa_7i_revenue_sharing_net_of_7j`, `fund_deposit`, `grant_from_resource_fund`, `ancsa_7i_revenue_sharing`, `ancsa_7j_redistribution`, `bonus`, `total_reported_revenue`, `ancsa_7i_7j_obligation_combined`, `production_tax_paid_to_state`, `payment_in_lieu_of_taxes`
- **`resource_type`** — `oil_and_gas`, `sand_and_gravel`, `coal`, `other_mineral`, `hardrock`, `mixed`, `geothermal`, `not_stated`
- **`mineral_lease_type`** — `Oil & Gas`, `Sand & Gravel`, `Coal`, `Copper`, `Gypsum`, `Geothermal`, `Cinders`, `Mining-Unspecified`, `Limestone`, `Phosphate`, `Chat`, `Hardrock`, `Silica Sand`
- **`period_type`** — `month`, `payment_date_only`, `federal_fiscal_year`, `corporate_fiscal_year`, `quarter`, `state_fiscal_year`, `payment_quarter`, `calendar_year`
- **`measurement_status`** — `reported_revenue`, `actual_payment`
- **`aggregation_level`** — `national_aggregate`, `entity_specific`, `state_aggregate`, `per_headright_rate`, `entity_specific_component`
- **`land_status`** — `not_stated`, `trust`
- **`allocation_formula`** — `STATUTORY SPLIT FOR NEW WELLS ONLY: trust lands 80% tribe / 20% state; non-trust (fee) lands 20% tribe / 80% state. THIS RATIO MUST NOT BE APPLIED TO THE SERIES. It governs only wells on which drilling first commenced after 2019-06-30; wells spudded earlier stay on the prior split for the life of the well, so every monthly distribution after this date is a BLEND of two regimes weighted by the spud-date mix of producing wells. The published distribution does not decompose by well vintage, and Cedar Press does not model the mix.`, `ANCSA Section 7(i): 70% of net revenues from timber resources and the subsurface estate is divided among all 12 Alaska-based regional corporations in proportion to original enrollment. ANCSA Section 7(j): not less than 50% of what a regional corporation receives under 7(i) must be redistributed to village corporations and at-large shareholders in its region. THIS FORMULA MUST NOT BE APPLIED TO ANY ROW - the shares are governed by the 1982 Section 7(i) Settlement Agreement and by allowable deductions and cost carryforwards that no report in this source system quantifies.`, `Trust lands: 50% tribe / 50% state. Non-trust (fee) lands: 50% tribe / 50% state. MATCHED ON PAYMENT DATE, which lags the taxable event, so payments near a boundary may be governed by the adjacent period.`, `Trust lands: 50% tribe / 50% state. Non-trust (fee) lands: 20% tribe / 80% state. MATCHED ON PAYMENT DATE, which lags the taxable event, so payments near a boundary may be governed by the adjacent period.`, `Distributed per full headright; the Osage Minerals Council prints a divisor of 2228.97393 headrights and rounds the rate back to the nearest $5. THE DIVISOR IS NOT APPLIED HERE - multiplying this rate by it would manufacture an aggregate.`, `Utah Code 59-5-116 (deposits) and 63N-24-601..606 (fund)`, `Utah Code 59-5-119 (deposits) and 63N-24-701..706 (fund)`, `Fort Peck agreement sec. VIII: the Tribes receive 50% of the total tax on NEW oil and NEW natural gas production on the Reservation, paid quarterly (sec. VII.B). 'New' means wells on which drilling commenced on or after the effective date; existing production is unaffected.`, `Total Revenue reproduces the published per-headright rate WITHOUT subtracting the Oklahoma gross production tax, so this vintage reports revenue NET of that tax.`, `Total Revenue reproduces the published per-headright rate only AFTER subtracting the Oklahoma gross production tax, so this vintage reports revenue GROSS of that tax. NOT COMPARABLE with the pre-2018 vintage without adjustment.`, `Neither arithmetic reproduces the published per-headright rate; the relationship between total revenue and the distribution is not established for this letter.`
- **`allocation_formula_effective_start`** — `2019-07-01`, `2013-07-01`, `2007-07-01`, `2008-03-25`
- **`allocation_formula_effective_end`** — `2019-06-30`, `2013-06-30`
- **`amount_sign_meaning`** — `negative = refund, recoupment or prior-period correction; retained, never dropped`, `negative = correction or recoupment`, `negative = recoupment or correction; retained`, `positive = amount the named corporation reports it RECEIVED`, `dollars per FULL headright for the quarter; not a total and not additive with any other row`, `negative = refund or prior-period correction; retained`, `positive = amount the named corporation reports it PAID OUT`, `positive = deposited into the fund`, `NEGATIVE AS PUBLISHED = money paid OUT of the fund; the sign is the source's and is retained so inflow and outflow do not have to be told apart by column name`, `a 'Major Details' line item that does NOT sum with its siblings to the quarter's stated total revenue; in 2016Q3 the oil line alone exceeds the total. Never add these to the total.`, `$0.00 is an assertion that nothing was distributed, NOT missing data`, `quarterly total revenue of the mineral estate as the Council states it`, `AN OUTFLOW. Oklahoma gross production tax assessed on the Osage royalty interest and paid BY the estate TO the state. Positive here means money left the estate; never add it to revenue.`
- **`geography_note`** — `State, county, FIPS and offshore region are BLANK on every Native American row by source design. Verified in this build, not assumed: 0 of 9,238 Native monthly revenue rows carry any geography, against 97.8% non-blank on Federal rows.`, `Payee is named; no well or tract is named.`, `Alaska. The report names no lease, tract, well or mine for the 7(i) flow itself; 7(i) is pooled across all shareable resources of the paying region.`, `Osage County, Oklahoma - the mineral estate is coextensive with the county. No well or lease is named.`, `State and county blank on every Native American disbursement row; from FY2015 the rows are split on an undisclosed dimension - see the codebook`, `Fund-level total for the whole fund; no well, tract or grantee is named.`, `National aggregate across all American Indian lands; no state, county or tribe.`, `Statewide letter; no tribe and no county is named on the tribal line.`, `Red Dog Mine, Northwest Arctic Borough, Alaska.`
- **`confidence`** — `A`, `B`
- **`fetched_date`** — `2026-08-06`, `2026-08-05`
- **`entity_id`** — `TRBF-MHATAT-00`, `PAYER-STATE-ND`, `TRBF-OSAGEN-00`, `TRBF-UNTHOR-00`, `TRBF-NAVAJO-00`, `ANRC-NANARC-00`, `ANRC-CKINLT-00`, `ANRC-ALEUTC-00`, `ANRC-CHGCCO-00`, `ANRC-ARCSLO-00`, `ANRC-BRBYCO-00`, `ANRC-KONIAG-00`, `ANRC-DOYONL-00`, `ANRC-SEALSK-00`, `ANRC-CALSTA-00`, `ANRC-BERSTR-00`, `ANRC-AHTNAI-00`
- **`entity_name`** — `Three Affiliated`, `State of North Dakota`, `The Osage Nation`, `Ute`, `Navajo`, `NANA Regional Corporation, Incorporated`, `Cook Inlet Region, Incorporated`, `Aleut Corporation`, `Chugach Alaska Corporation`, `Arctic Slope Regional Corporation`, `Bristol Bay Native Corporation`, `Koniag, Incorporated`, `Doyon, Limited`, `Sealaska Corporation`, `Calista Corporation`, `Bering Straits Native Corporation`, `Ahtna, Incorporated`
- **`party_role`** — `recipient`, `payer`, `fund_beneficiary_population`, `mineral_estate_owner`
- **`relationship`** — `parent_native_entity`, `counterparty`, `serves_native_entities`
- **`basis`** — `the distribution record names the payee; resolve_entity/core`, `the State Treasurer is the payer of record`, `the corporation's own annual report states this amount about itself; resolve_entity/exact`, `the fund is fed by severance tax on interests held in trust for this tribe and serves that population; the tribe does NOT own the fund`, `1906 Osage Allotment Act reserved the mineral estate to the Osage Nation undivided; published by the Nation's own Minerals Council; resolve_entity/exact`, `1906 Osage Allotment Act; published by the Nation's own Minerals Council; resolve_entity/exact`
