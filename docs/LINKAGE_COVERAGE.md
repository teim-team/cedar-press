# Linkage coverage - what fraction of each shipped dataset can be attributed to a Native entity

*GENERATED by `code/1139_linkage_coverage.py apply`. Measured 2026-09-02 against `data/clean` directly rather than against `dist/customer`, so it cannot collide with a build in progress. **Do not hand-edit - re-run the script.***

**Every percentage here states its denominator in the row beneath it.** This project has shipped percentages whose denominator nobody stated, and one gaming count circulated in five values in a single day (`docs/AGENT_FIELD_GUIDE.md` rule 15). The `LINKED` column is always the CONJUNCTION - a key column is populated **and** no gate column on the same row withdraws it - so it is the smallest defensible reading. Where a sibling column gives a different answer the difference is printed, in rows, in that dataset's own section.

| dataset | flagship | rows (denominator) | LINKED | % | unlinked | % of rows that CAN name an entity |
|---|---|---:|---:|---:|---:|---:|
| `contractors` | `prime_contracts.csv` | 1,217,768 | 791,394 | 64.99% | 426,374 | 68.50% of 1,153,140 |
| `subcontracting` | `subawards.csv` | 89,809 | 87,355 | 97.27% | 2,454 | — |
| `funding` | `federal_funding_transactions.csv` | 701,955 | 549,136 | 78.23% | 152,819 | 78.47% of 699,836 |
| `gaming` | `gaming_facilities.csv` | 787 | 785 | 99.75% | 2 | — |
| `natural-resources` | `resource_revenue.csv` | 11,305 | 705 | 6.24% | 10,600 | 73.67% of 957 |
| `native-owned-businesses` | `native_owned_businesses.csv` | 2,916 | 2,767 | 94.89% | 149 | — |
| `nonprofits` | `np_orgs.csv` | 12,764 | 1,423 | 11.15% | 11,341 | 18.23% of 7,804 |
| `deals` | `deals_classified.csv` | 1,073 | 959 | 89.38% | 114 | — |
| `lobbying` | `native_entity_lobbying_disclosures.csv` | 27,825 | 26,513 | 95.28% | 1,312 | — |
| `legislation` | `native_bills.csv` | 3,069 | 591 | 19.26% | 2,478 | — |
| `federal-register` | `consultation_events.csv` | 11,402 | 10,396 | 91.18% | 1,006 | — |
| `nagpra` | `nagpra_notices.csv` | 6,792 | 6,169 | 90.83% | 623 | — |
| `nest` | `nest_enterprises.csv` | 4,798 | 4,798 | 100.00% | 0 | — |

**Across the 13 measured flagships: 1,482,991 of 2,092,263 rows (70.88%) carry a resolved Cedar entity.** That total sums tables whose rows are not the same kind of thing - a contract award and a NAGPRA notice each count as one - so it is a SCALE figure and never a quality figure. Quote the per-dataset rows.

---

## `contractors` - `prime_contracts.csv`

**Denominator: 1,217,768 rows.** One row is one prime contract award row (FPDS / USAspending), FY2000-2026.

**Third denominator - the one that says whether a low figure is a DEFECT.** 64,628 rows can never name an individual Cedar entity, because an owner ruling says the awardee is not a Native entity, or that the award is a CLASS-level fact naming no individual entity. `RULED_OWNER_NOT_IN_SPINE` is deliberately NOT counted here - that one IS a Cedar gap (`ruling_status IN ('RULED_NOT_NATIVE','RULED_CLASS_ONLY')`). Against the 1,153,140 rows that CAN, this dataset is **68.50%** linked. The ratchet still runs on the raw 64.99%, so this reading can never be used to make a real fall look like a change of definition.

**LINKED: 791,394 (64.99%)** - `coalesce(trim(CAST(tribe_id AS VARCHAR)),'') <> '' AND attributed_flag = '1'`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `key_only:tribe_id` | 791,490 | +96 |
| `gate_only:attributed_flag` | 791,394 | +0 |
| `display_only:cedar_uid` | 791,490 | +96 |

**Money on unlinked rows: $79,760,452,306.21 of $310,005,258,660.75** in `total_obligations` (25.73%). This sums a column as recorded and is subject to `docs/MONEY_TOTALLING_RULES.md`. It measures EXPOSURE - how much money is not attributable - and is not a Cedar total of anything.

**The largest unlinked residue, by row count:**

| name as recorded | rows | amount |
|---|---:|---:|
| BLUE TECH INC. | 32,248 | $3,505,660,428.98 |
| PERATON GOVERNMENT COMMUNICATIONS INC. | 20,887 | $1,686,751,008.40 |
| Fleet Vehicle Source, Inc. | 7,759 | $203,671,167.00 |
| Jit Industries, Inc. | 6,392 | $7,839,675.37 |
| IDS INTERNATIONAL, LLC | 6,367 | $1,080,762,007.76 |

## `subcontracting` - `subawards.csv`

**Denominator: 89,809 rows.** One row is one subaward row. LINKED if EITHER side (prime or subawardee) resolves, because either one makes the row attributable.

**LINKED: 87,355 (97.27%)** - `(coalesce(trim(CAST(prime_cedar_uid AS VARCHAR)),'') <> '' OR coalesce(trim(CAST(sub_cedar_uid AS VARCHAR)),'') <> '')`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `subawardee_side` | 49,404 | -37,951 |
| `prime_side` | 39,684 | -47,671 |
| `row_level:cedar_uid` | 40,201 | -47,154 |

**Money on unlinked rows: $1,662,416,606.58 of $57,020,557,710.47** in `subaward_amount` (2.92%). This sums a column as recorded and is subject to `docs/MONEY_TOTALLING_RULES.md`. It measures EXPOSURE - how much money is not attributable - and is not a Cedar total of anything.

## `funding` - `federal_funding_transactions.csv`

**Denominator: 701,955 rows.** One row is one federal assistance transaction, FY2007-2026.

**Third denominator - the one that says whether a low figure is a DEFECT.** 2,119 rows can never name an individual Cedar entity, because the recipient is ruled not a Native entity (`attribution_status = 'excluded_not_native'`). Against the 699,836 rows that CAN, this dataset is **78.47%** linked. The ratchet still runs on the raw 78.23%, so this reading can never be used to make a real fall look like a change of definition.

**LINKED: 549,136 (78.23%)** - `coalesce(trim(CAST(tribe_id_neid AS VARCHAR)),'') <> '' AND attribution_status = 'cedar_neid' AND attributed_flag = '1'`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `key_only:tribe_id_neid` | 552,756 | +3,620 |
| `gate_only:attribution_status` | 552,756 | +3,620 |
| `gate_only:attributed_flag` | 549,180 | +44 |

**Money on unlinked rows: $51,534,695,533.83 of $219,689,020,478.59** in `obligated_usd` (23.46%). This sums a column as recorded and is subject to `docs/MONEY_TOTALLING_RULES.md`. It measures EXPOSURE - how much money is not attributable - and is not a Cedar total of anything.

**The largest unlinked residue, by row count:**

| name as recorded | rows | amount |
|---|---:|---:|
| BUREAU OF INDIAN AFFAIRS | 9,811 | $12,917,145.10 |
| UNITED TRIBES TECHNICAL COLLEGE | 2,875 | $286,515,987.04 |
| DENA NENA HENASH | 2,496 | $1,783,253,649.45 |
| WINNEBAGO COUNTY HOUSING AUTHORITY | 2,205 | $179,736,810.75 |
| MANCHESTER HOUSING & REDEVELOPMENT AUTHORITY | 1,988 | $520,147,681.00 |

## `gaming` - `gaming_facilities.csv`

**Denominator: 787 rows.** One row is one gaming FACILITY row - not one property. The 787 rows resolve to 714 distinct properties; the gated denominator ladder is code/846_session_audit.py::_denom.

**LINKED: 785 (99.75%)** - `coalesce(trim(CAST(cedar_uid AS VARCHAR)),'') <> ''`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `key_only:tribe_id` | 785 | +0 |

**The largest unlinked residue, by row count:**

| name as recorded | rows |
|---|---:|
| Konkow Valley Band - no casino | 1 |
| Golden Eagle Casino | 1 |

## `natural-resources` - `resource_revenue.csv`

**Denominator: 11,305 rows.** One row is one resource revenue event row.

**Third denominator - the one that says whether a low figure is a DEFECT.** 10,348 rows can never name an individual Cedar entity, because ONRR and the state publishers report Indian Country revenue in AGGREGATE and never name a recipient, and a further class of row has a recipient that is a category rather than an entity. These are SOURCE_DOES_NOT_PUBLISH - a fact about the world, never a Cedar deficiency (AGENT_FIELD_GUIDE section 5) - and keying them would be fabrication (`entity_attribution_status IN ('aggregate_suppressed_by_publisher', 'class_recipient_never_an_individual')`). Against the 957 rows that CAN, this dataset is **73.67%** linked. The ratchet still runs on the raw 6.24%, so this reading can never be used to make a real fall look like a change of definition.

**LINKED: 705 (6.24%)** - `coalesce(trim(CAST(cedar_uid AS VARCHAR)),'') <> ''`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `key_only:recipient_entity_id` | 705 | +0 |
| `status:keyed_to_cedar_entity` | 705 | +0 |

**Money on unlinked rows: $44,993,300,565.36 of $50,973,259,111.49** in `amount_usd` (88.27%). This sums a column as recorded and is subject to `docs/MONEY_TOTALLING_RULES.md`. It measures EXPOSURE - how much money is not attributable - and is not a Cedar total of anything.

## `native-owned-businesses` - `native_owned_businesses.csv`

**Denominator: 2,916 rows.** One row is one directory listing of a Native-owned business.

**Second denominator, also correct:** the customer file carries only `publishable = Y`, which is 2,044 rows, of which 1,895 are LINKED (92.71%). Say which denominator you used; the two differ by 872 rows.

**Which entity the link names:** the CERTIFYING NATION whose TERO, member directory or licence register the listing comes from. NOT the firm: these firms are owned by PEOPLE (identity_scope any_native 1,567 / citizen 385 / shareholder_descendant_or_spouse 98; 280 rows' names ARE natural persons), a sole proprietor is not a spine entity, and `business_entity_id` is populated on 4 rows precisely because the resolver refuses to manufacture one.

**LINKED: 2,767 (94.89%)** - `coalesce(trim(CAST(certifying_authority_entity_id AS VARCHAR)),'') <> ''`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `the_firm_itself:business_entity_id` | 4 | -2,763 |
| `federal_crosswalk:federal_link_status=LINKED` | 203 | -2,564 |

**The largest unlinked residue, by row count:**

| name as recorded | rows |
|---|---:|
| Cates & Erb Inc. | 1 |
| CG Contractors LLC | 1 |
| Davis Construction & Excavation | 1 |
| K&B Excavating LLC | 1 |
| Minahan Cutting, Inc. | 1 |

## `nonprofits` - `np_orgs.csv`

**Denominator: 12,764 rows.** One row is one nonprofit organisation (EIN filer).

**Third denominator - the one that says whether a low figure is a DEFECT.** 4,960 rows can never name an individual Cedar entity, because `EXCLUDED_PRIOR_RULING` (4,681) and `EXCLUDED_PLACE_NAME_COINCIDENCE` (279) are rows a ruling has already decided are NOT the entity a name matcher proposed. They are correctly unkeyed and they are not a gap (`disposition LIKE 'EXCLUDED%'`). Against the 7,804 rows that CAN, this dataset is **18.23%** linked. The ratchet still runs on the raw 11.15%, so this reading can never be used to make a real fall look like a change of definition.

**LINKED: 1,423 (11.15%)** - `coalesce(trim(CAST(tribe_id AS VARCHAR)),'') <> '' AND coalesce(trim(CAST(cedar_uid AS VARCHAR)),'') <> ''`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `key_only:tribe_id` | 1,423 | +0 |
| `spine_side:cedar_spine_entity_id` | 1,456 | +33 |

**The largest unlinked residue, by row count:**

| name as recorded | rows |
|---|---:|
| FARMERS EDUCATIONAL & COOPERATIVE UNION OF AMERICA NORTH DAKOTA DIV | 65 |
| SOUTH DAKOTA CONGRESS OF PARENTS AND TEACHERS | 41 |
| ASSOCIATION OF OLD CROWS | 41 |
| ORDER OF THE EASTERN STAR OF SOUTH DAKOTA | 28 |
| VETERANS OF FOREIGN WARS OF THE UNITED STATES DEPT OF NORTH DAKOTA | 28 |

## `deals` - `deals_classified.csv`

**Denominator: 1,073 rows.** One row is one classified deal / transaction row.

**LINKED: 959 (89.38%)** - `coalesce(trim(CAST(native_party_entity_id AS VARCHAR)),'') <> '' AND coalesce(trim(CAST(cedar_uid AS VARCHAR)),'') <> ''`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `key_only:native_party_entity_id` | 959 | +0 |

**Money on unlinked rows: $5,318,739,389.00 of $47,880,355,533.49** in `Announced_Value_USD` (11.11%). This sums a column as recorded and is subject to `docs/MONEY_TOTALLING_RULES.md`. It measures EXPOSURE - how much money is not attributable - and is not a Cedar total of anything.

**The largest unlinked residue, by row count:**

| name as recorded | rows | amount |
|---|---:|---:|
| Tunica-Biloxi | 6 | $0.00 |
| Mohegan Tribe of Indians of Connecticut (Mohegan Tribal Gaming Authori | 4 | $638,913,000.00 |
| Shee Atika, Incorporated | 4 | $18,300,000.00 |
| Cook Inlet Housing Authority | 3 | $17,000,000.00 |
| Natives of Kodiak, Incorporated | 3 | $5,999,500.00 |

## `lobbying` - `native_entity_lobbying_disclosures.csv`

**Denominator: 27,825 rows.** One row is one LDA disclosure filing row.

**LINKED: 26,513 (95.28%)** - `coalesce(trim(CAST(entity_id AS VARCHAR)),'') <> '' AND coalesce(trim(CAST(cedar_uid AS VARCHAR)),'') <> ''`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `key_only:entity_id` | 26,513 | +0 |

**Money on unlinked rows: $45,182,334.00 of $725,743,974.52** in `spend_usd` (6.23%). This sums a column as recorded and is subject to `docs/MONEY_TOTALLING_RULES.md`. It measures EXPOSURE - how much money is not attributable - and is not a Cedar total of anything.

**The largest unlinked residue, by row count:**

| name as recorded | rows | amount |
|---|---:|---:|
| SALT RIVER PROJECT | 324 | $28,708,000.00 |
| BRISTOL BAY ECONOMIC DEVELOPMENT CORPORATION | 129 | $1,998,500.00 |
| SANTA ROSA COUNTY FL | 106 | $1,495,000.00 |
| TULE RIVER IMPROVEMENT JPA (FORMERLY LOWER TULE RIVER IRRIGATION DISTR | 98 | $430,000.00 |
| BRISTOL BAY AREA HEALTH CORP | 97 | $480,000.00 |

## `legislation` - `native_bills.csv`

**Denominator: 3,069 rows.** One row is one bill.

**LINKED: 591 (19.26%)** - `has_resolved_entity = '1'`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `legacy_column:affected_entities` | 0 | -591 |

**The largest unlinked residue, by row count:**

| name as recorded | rows |
|---|---:|
| Unrecognized Southeast Alaska Native Communities Recognition and Compe | 17 |
| Tribal Adoption Parity Act | 12 |
| Endangered Species Management Self-Determination Act | 9 |
| Alaska Native Veterans Land Allotment Equity Act | 9 |
| To grant a Federal charter to the National American Indian Veterans, I | 8 |

## `federal-register` - `consultation_events.csv`

**Denominator: 11,402 rows.** One row is one Federal Register consultation event.

**LINKED: 10,396 (91.18%)** - `coalesce(trim(CAST(tribe_id AS VARCHAR)),'') <> '' AND coalesce(trim(CAST(cedar_uid AS VARCHAR)),'') <> ''`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `key_only:tribe_id` | 10,396 | +0 |

## `nagpra` - `nagpra_notices.csv`

**Denominator: 6,792 rows.** One row is one NAGPRA Federal Register notice.

**Which entity the link names:** ANY of six party roles. This table has NO single-id column - the keys are six PIPE-DELIMITED list columns, because one notice names many parties. LINKED is their union.

**This table's key is LIST-VALUED.** No single-id column exists; LINKED is the union of `consulted_entity_ids`, `affiliated_entity_ids`, `disposition_priority_entity_ids`, `repatriation_recipient_entity_ids`, `letter_of_support_entity_ids`, `aboriginal_land_entity_ids`. A scan looking only for `cedar_uid` / `tribe_id` / `entity_id` reports 0% here and is wrong.

**LINKED: 6,169 (90.83%)** - `(coalesce(trim(CAST(consulted_entity_ids AS VARCHAR)),'') <> '' OR coalesce(trim(CAST(affiliated_entity_ids AS VARCHAR)),'') <> '' OR coalesce(trim(CAST(disposition_priority_entity_ids AS VARCHAR)),'') <> '' OR coalesce(trim(CAST(repatriation_recipient_entity_ids AS VARCHAR)),'') <> '' OR coalesce(trim(CAST(letter_of_support_entity_ids AS VARCHAR)),'') <> '' OR coalesce(trim(CAST(aboriginal_land_entity_ids AS VARCHAR)),'') <> '')`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `derived_flag:has_resolved_entity` | 6,169 | +0 |
| `affiliated_role_only` | 5,022 | -1,147 |
| `consulted_role_only` | 2,698 | -3,471 |
| `repatriation_recipient_role_only` | 1,625 | -4,544 |

**The largest unlinked residue, by row count:**

| name as recorded | rows |
|---|---:|
| Peabody Museum of Archaeology and Ethnology, Harvard University | 31 |
| American Museum of Natural History | 13 |
| California Department of Transportation | 11 |
| University of Nevada, Las Vegas | 9 |
| Tennessee Valley Authority | 8 |

## `nest` - `nest_enterprises.csv`

**Denominator: 4,798 rows.** One row is one tribally / ANC / NHO-owned enterprise. The owner hub is this table's KEY, so coverage is 100% by construction; the informative figure is the alternative below - how many enterprises also carry their OWN spine entity.

**LINKED: 4,798 (100.00%)** - `coalesce(trim(CAST(owner_hub_cedar_uid AS VARCHAR)),'') <> ''`

| alternative reading | rows | apart from LINKED |
|---|---:|---:|
| `enterprise_own_entity:enterprise_existing_cedar_uid` | 67 | -4,731 |

---

## The ratchet

`py -3 code/1139_linkage_coverage.py baseline` records the figures above as a floor in `data/clean/_linkage_coverage_baseline.json`, and `verify` exits 1 when any dataset falls more than 25 basis points (0.25 pp) below it. `code/62_no_regression_check.py` carries the same figures as `linkage_<dataset>_bp` (MUST_NOT_FALL) by importing `metrics()` from this file, so there is ONE measurement and not two - `248` is a retired stub for exactly the reason that two detectors for one class drift.

**The tolerance is not zero on purpose.** Several flagships are rebuilt by other workstreams, and a rebuild that adds honest unlinked rows lowers the ratio without losing a single link. Losing links cannot hide inside it: 25 bp of `prime_contracts` is more than 3,000 rows. **`linkage_<dataset>_rows` is carried beside it with NO tolerance at all**, so a fall in the absolute count of linked rows fails the gate even when the ratio holds.

