# Subcontracting (Dataset 2b) — COVERAGE LEDGER

*Hand-maintained. Owner of the numbers: `code/121_pull_subawards_api.py`.
Last measured **2026-09-01** by workstream L.*

> This file exists so nobody re-derives the answer to "do we have all the
> years?" again. `docs/datasets/02b_subcontracting.md` is GENERATED by
> `code/24_generate_dataset_docs.py` and describes what the dataset *is*; this
> file is written by hand and records what it *covers* and why the edges are
> where they are.

---

<!-- CEDAR:COVERAGE-MEASURED collection=subcontracting START -->

## Readiness and coverage — measured, never hand-typed

*The status line and the `Years Cedar holds` column below are regenerated by `py -3 code/621_dataset_coverage.py` (tables measured 2026-09-02) and `py -3 code/518_dataset_readiness.py`. Do not edit them by hand; edit the table and re-run. The `Years upstream` research around this block is authored and is NOT touched by the generator.*

**Status: BLOCKED** — 3 customer tables, shelf `pro`, measured 2026-09-02 by `518`.

**Open contract points, with the tables that carry them:**

- C1 grain UNSTATED on 1: subawards.csv
- C2 no validated primary key on 1
- C3 literal duplicates: subawards.csv(10,770)
- C7 DOUBLE-COUNTING RISK - money tables a buyer cannot safely total: subawards.csv

Next action: **C1 grain UNSTATED on 1: subawards.csv**.

| contract point | state |
|---|---|
| C1 grain stated | 2/3 |
| C2 validated primary key | 2/3 |
| C3 literal duplicates | 10,770 rows |
| C4 identity path | 100% keyed |
| C5 row conservation | 1/3 |
| C6 unresolved conflicts | 0 shipped as definite |
| C7 double counting | 1 tables |
| C8 rebuild path | declared |
| C9 update documented | see docs/datasets/ |
| C10 gates | 62 + semantic diff |

### Years Cedar holds — per table, measured from the live file

| Table | Rows | Period column | Years Cedar holds |
|---|---:|---|---|
| `prime_sub_network.csv` | 220 | — | no dated column |
| `subaward_entity_rollup.csv` | 450 | — | no dated column |
| `subaward_identifier_harvest.csv` *(internal-by-decision)* | 304 | — | no dated column |
| `subaward_identifier_netnew.csv` *(internal-by-decision)* | 210 | — | no dated column |
| `subawards.csv` | 76,859 | `fiscal_year` | 2001–2026 (interior gap: 2005, 2006) · **thin: 2002, 2003, 2004, 2007, 2008, 2009, +3 more** |

<!-- CEDAR:COVERAGE-MEASURED collection=subcontracting END -->

## STATUS 2026-09-02 — FY2023 IS HALF LANDED; FY2022 AND FY2024 ARE NOT

*Workstream DOCS measured, correctly, on 2026-09-01 that nothing had reached the
clean table. That has changed for FY2023 and has NOT changed for FY2022 or
FY2024. Superseding that note rather than deleting it, because its warning
still binds for the two years that are still empty.*

**Landed and verified 2026-09-02 02:38Z** (`121 match` then `121 append`):

| FY | rows before | rows after | USD before* | USD after* |
|---|---:|---:|---:|---:|
| 2021 | 9,462 | 9,504 | $4,816,365,423 | $4,823,715,821 |
| **2023** | **120** | **4,100** | **$93,394,904** | **$1,537,605,212** |
| 2022 | 89 | **89 — UNCHANGED** | $47,021,525 | unchanged |
| 2024 | 166 | **166 — UNCHANGED** | $113,334,471 | unchanged |

\* after BOTH money filters: `duplicate_status == 'primary'` AND
`subaward_exceeds_prime_flag != 'yes'`.

**Every other fiscal year is unchanged in rows and in dollars**, and all 72,837
pre-existing rows were verified byte-identical on all 53 of their original
columns against the pre-run backup. The FY2021 +42 is not a re-pull artefact:
the same raw zip re-read against a ledger that has gained entities since
2026-08-28 now resolves 42 rows it previously could not.

FY2023 is **half** the year — Q1 and Q2 (2022-10-01..2023-03-31), 361,109 raw
rows. Q3 and Q4 were still outstanding when this was written.

**Do not quote an FY2022 or FY2024 subcontracting figure.** Those years still
hold only HigherGov and forward-fill rows and no FSRS data at all. FY2023 may
now be quoted as **a half-year, explicitly labelled as such** — it is not
comparable to a full neighbour year.

### Why the full-year jobs keep dying, measured rather than guessed

Three explanations were on the table and two are disproved by data in this repo:

| explanation | verdict |
|---|---|
| "a full fiscal year is too big for the async job" | **DISPROVED.** `fy2021` is a full fiscal year, 765,109 rows, same endpoint, same payload, and it FINISHED in 23,613s on 2026-08-26. `fy2022` reached 221,865 rows before it died. Meanwhile a *two-day* canary FAILED on 2026-08-12. Size does not predict the outcome. |
| "the job is dead because it has sat at 0 rows for 80 minutes" | **DISPROVED.** The server reports `rows_so_far=0` until it has built the file. `fy2021` did that for almost all of its 393 minutes and then completed. A job at zero rows is EARLY, not dead — killing it discards completed server work (rule 5). |
| "the bulk archive has subawards, use that instead" | **DISPROVED, and already enumerated.** `docs/ASSISTANCE_ARCHIVE_PULL_LOG.md`: the archive holds **4,631 objects and ZERO of them contain the string `sub` in any case**; direct probes of every plausible subaward path returned 404 or 403. `files.usaspending.gov` publishes Contracts and Assistance only. Do not re-spend host budget re-probing this. |

What the data actually shows is a **periodic server-side kill**. Jobs died in
pairs at two instants — `fy2023` and `fy2024` together at 00:07:14Z, then
`fy2022` and `fy2023_q3` together at 02:36:10Z — while everything that
COMPLETED did so between those instants: the canary (536s), `fy2023_q1`
(4,087s) and `fy2023_q2` (2,809s). The practical rule that follows is not
"slice for size" but **"finish inside the window"**: a job under ~70 minutes
lands, a job that has to survive several hours does not.

That is why quarters work, and it is the only reason they work. They are a
scheduling hedge, not a size fix.

---

## 1. THE COVERAGE TABLE

`available` = what the source will serve today. `held` = what
`data/clean/subawards.csv` actually contains.

| source | earliest available | earliest held | latest available | latest held | gap | why the gap |
|---|---|---|---|---|---|---|
| **USAspending FSRS** — `POST /api/v2/bulk_download/awards/`, `sub_award_types=[procurement, grant]`, no recipient filter | FY2010 (real floor) | FY2010 | FY2026, live; filings arrive continuously | FY2026, as-of 2026-08-06 | **FY2022–FY2024 and FY2020-contracts were the gap; closed by the 2026-09-01 run** | see §2, one row per year |
| **HigherGov 2023 export** — `subcontract-05-09-23-22-23-37.csv` | FY2011 | FY2011 | FY2023 (export is frozen) | FY2023 | none, and none possible | A one-off vendor export, superseded. Its query definition was never preserved, so its sampling frame is unknown and no share-of-market claim can rest on it. 998 rows. **Not refreshable — its absence after FY2023 is not a gap.** |
| **Federal-funding forward-fill** — `Assistance_Subawards_*.csv`, spill-over from the Dataset 3 pull | FY2023 | FY2023 | FY2026 | FY2026 | none | 608 rows, and **a different population**: that pull filtered `recipient_type_names=indian_native_american_tribal_government` on the PRIME, so the prime is Native by construction and the file cannot observe direction (b) at all. Carried as `source_population=prime_tribal_filtered`. It is not a subaward source; it is a by-product of another one. |

### The three edges, and which are ours

| edge | verdict |
|---|---|
| **Nothing before FY2010** | **COMPLETE, not missing.** FFATA dropped the subaward reporting threshold from $25M to $25,000 in October 2010, so FSRS has nothing to report before then. Demonstrated, not assumed: the FY2001–FY2009 jobs returned 4,945 raw rows in total and **every one carries `subaward_sam_report_year >= 2010`** — the pre-2010 `subaward_action_date` values are filer typos, including a SpaceX subaward dated 2000-11-09 and filed in 2024. The 51 such rows in `subawards.csv` are flagged `action_date_precedes_ffata_flag = yes` and must never be counted as coverage. **They are not HigherGov rows** — all 51 come from the USAspending FSRS pull, and the HigherGov export starts at FY2011. |
| **FY2026 is incomplete** | **COMPLETE FOR ITS AS-OF DATE.** The fiscal year runs to 2026-09-30 and cannot be closed. Held as-of 2026-08-06; the `fy2026_refresh` job moves the as-of date forward. |
| **FY2022–FY2024, and FY2020 contract subawards** | **OURS.** Two separate failures, both addressed by the 2026-09-01 run. See §2. |

---

## 2. WHAT WAS ACTUALLY WRONG, PER YEAR

Row counts below are the SOURCE's own `total_rows` for the full-universe
fiscal-year job — the honest denominator. `subawards.csv` holds only the
Native-linked subset of them.

| FY | full-universe rows the source served | pulled on | note |
|---|---:|---|---|
| 2001–2009 | 4,945 total | 2026-08-05 | misdated filings only; see above |
| 2010 | 13,501 | 2026-08-05 | FFATA phase-in year |
| 2011 | 326,644 | 2026-08-05 | |
| 2012 | 517,635 | 2026-08-05 | |
| 2013 | 569,137 | 2026-08-05 | |
| 2014 | 682,396 | 2026-08-05 | |
| 2015 | 644,594 | 2026-08-05 | |
| 2016 | 658,992 | 2026-08-05 | |
| 2017 | 519,998 | 2026-08-05 | |
| 2018 | 738,115 | 2026-08-05 | |
| 2019 | 678,817 | 2026-08-06 | contracts member 439 MB |
| **2020** | 456,412 | 2026-08-06 | **THE CONTRACTS MEMBER CAME BACK EMPTY — 4,144 bytes, one header line, zero data rows.** Every other year's contracts member is 60–470 MB. The 456,412 rows are ALL assistance. `subawards.csv` therefore holds zero FSRS contract subawards for FY2020, against neighbours of 9,254 (FY2019) and 9,289 (FY2021) FSRS rows; the only FY2020 contract rows in the file are 180 inherited HigherGov rows. Re-submitted as `fy2020_procurement` — procurement-only, so an empty answer is unambiguous. |
| 2021 | 765,109 | **2026-08-26** | backfilled late. This is why FY2021 looks normal while FY2022–24 do not — it is not evidence that FY2022–24 are genuinely thin. |
| **2022** | see §6 | **2026-09-01 (this run)** | **NEVER SUBMITTED in the original pull.** The 2026-08-12 attempt was accepted and then failed server-side in a service-wide outage. Before this run `subawards.csv` held 89 FY2022 rows, every one of them HigherGov. Zero FSRS. |
| **2023** | see §6 | **2026-09-01 (this run)** | same failure. Before: 120 rows — 96 forward-fill, 24 HigherGov. Zero FSRS. |
| **2024** | see §6 | **2026-09-01 (this run)** | same failure. Before: 166 rows, all forward-fill. Zero FSRS. |
| 2025 | 516,151 | 2026-08-05 | a closed year, but still gaining late filings — `fy2025_refresh` |
| 2026 | 286,134 | 2026-08-06 | open year, as-of 2026-08-06 — `fy2026_refresh` |

### Why a closed year still moves

FSRS filings arrive years after the action date. Measured on one FY2021
subaward: **93 separate SAM reports on a single $57,500 subaward, filed monthly
from 2022-08 through 2025-01.** So "we pulled FY2019 once" is not the same claim
as "we hold FY2019", and a re-pull of a closed window is not a duplicate pull.
`fy2025_refresh` and `fy2026_refresh` exist for exactly this, and they are
deduped by `m45.identity_key` rather than stacked — re-reading a window is
idempotent by construction.

---

## 3. THE ROW KEY — measured 2026-09-01, and it cuts both ways

`code/525_event_ids.py` registers subcontracting as the **one dataset that
genuinely lacks an event id**, and closes: *diagnose the source extract first.*
Diagnosed, at zero network cost, on extracts already on disk.

**The source HAS a unique row id, and the mapper drops it.**

* The FSRS extract carries **121 columns**. `94_match_raw_subawards.build_row`
  reads **26** of them. Ninety-five are dropped.
* `subaward_sam_report_id` is one of the dropped ones, and it is a UUID that is
  **globally unique**:

| | rows | distinct `subaward_sam_report_id` | blank |
|---|---:|---:|---:|
| FY2021, both zip members | 765,109 | **765,109** | 0 |
| FY2020, both zip members | 456,412 | **456,412** | 0 |
| FY2020 ∩ FY2021 | — | **0 overlap** | — |

This is the `prime_contracts` shape exactly: 80,778 "duplicates" there turned
out to be distinct transactions whose `modification_number` the mapper never
carried.

**AND YET THE DE-DUPLICATION IS SUBSTANTIVELY RIGHT.** A unique id on the row
does not make the rows distinct subawards:

* `m45.identity_key` collides on **111,933 of 765,109** FY2021 raw rows (14.6%).
* The worst group is **93 rows**. All 93 carry distinct
  `subaward_sam_report_id` values. Their `subaward_sam_report_month` values run
  2022-08 through 2025-01, on **one** $57,500 subaward with one action date and
  one subaward number.
* They are **93 monthly re-filings of one subaward**, not 93 subawards.
  Counting them would inflate the year by roughly 14.6% of rows.

So `subaward_sam_report_id` identifies **a SAM subaward REPORT, not a
subaward**, and the two grains must not be confused:

| if you want | the key is | status |
|---|---|---|
| one row per SAM **report** | `subaward_sam_report_id` | exists in the source, **not carried** |
| one row per **subaward** | `m45.identity_key` plus `duplicate_status == 'primary'` | carried, and correct |

**Do NOT mint a surrogate over `subaward_sam_report_id` and call it a subaward
id.** 525's warning still binds: the id is real, but it counts reports.

**What carrying the three report columns WOULD buy**, and why it deserves a
later pass: `duplicate_status` is today *inferred* from a six-field tuple and
cannot be checked against the source without re-reading a 1.2 GB zip. With
`subaward_sam_report_id`, `subaward_sam_report_month` and
`subaward_sam_report_last_modified_date` on the row, it becomes auditable, and
"is this a re-filing or a second subaward?" becomes a lookup instead of an
argument.

**Not done in the 2026-09-01 run, deliberately.** It changes the grain
declaration of a shipping table and belongs with whoever owns
`code/525_event_ids.py` and the schema, not with a puller.

### Other columns the mapper drops that this source would give us

`subawardee_duns` and `prime_awardee_duns` — DUNS is recoverable here for the
pre-2022 period, and the 2023 HigherGov export had no DUNS column at all;
sub-side city, zip and place-of-performance; congressional districts, original
and current; `prime_award_cfda_numbers_and_titles` on the assistance member;
prime funding agency / office and federal-account strings; COVID-19 and IIJA
supplemental amounts; and the five
`subawardee_highly_compensated_officer_*` name/amount pairs.

---

## 4. HOW TO REFRESH, AND WHAT NOT TO RUN

```
py -3 code/121_pull_subawards_api.py canary            # ~9 min, one 2-day job
py -3 code/121_pull_subawards_api.py pull --only <jobs>
py -3 code/121_pull_subawards_api.py collect           # resume accepted tokens
py -3 code/121_pull_subawards_api.py match
py -3 code/121_pull_subawards_api.py append            # the enricher, runs LAST
py -3 code/250_demote_stale_tierA_subaward_rows.py     # the other 2b enricher
```

* **Buy the cheap answer first.** `canary` is one two-day job. On 2026-08-12 the
  API returned HTTP 200 and a token for nine consecutive submissions and built
  none of them. An accepted token is not a working job.
* **`45_promote_subawards.py` CANNOT promote a 121 pull, and running it is
  destructive.** It reads only
  `data/raw/subcontracts/usaspending_subawards_2026-08-05` and cannot see
  `usaspending_2026-08-12/`; it would re-stamp `source_dataset` on rows it
  re-reads, and write 49 columns, reverting the deflator enrichment. The route
  for anything 121 pulls is **`121 match` then `121 append`**. The generated
  `02b_subcontracting.md` still says to run 41 then 45; that instruction is
  stale for this pull path.
* One poller per host, ever — `docs/PULL_DISCIPLINE.md`. Check with
  `Win32_Process.CommandLine`; `ps aux` cannot see command lines on Windows and
  has returned 0 with four pullers live.

---

## 5. WHAT IS STILL OPEN

| item | why it matters |
|---|---|
| carry `subaward_sam_report_id` / `_month` / `_last_modified_date` | closes the 525 MISSING registration at the *report* grain and makes `duplicate_status` auditable. Needs the schema owner, not a puller. |
| `subawardee_duns` / `prime_awardee_duns` | recoverable from this exact source for the pre-2022 period; the HigherGov export has none. |
| keep the full-universe selection | 121 pulls the **whole federal subaward universe** with no recipient filter and attributes afterwards, so it does not carry the identifier-only blind spot `docs/PULL_DISCIPLINE.md` sizes at roughly three quarters of the entity universe. That is a strength of this dataset and must not be "optimised" into a UEI-seeded pull. |

---

## 6. RUN LOG

| date | what | outcome |
|---|---|---|
| 2026-08-05/06 | FY2001–FY2020, FY2025, FY2026 | landed; FY2020 contracts member empty |
| 2026-08-12 | FY2021–FY2024 + FY2020 procurement | all accepted, all failed server-side. `diagnose` verdict A: the whole `bulk_download` service was building nothing that day. Not our rate, not our payload. |
| 2026-08-26 | FY2021 | canary passed, FY2021 landed (765,109 raw rows, 23,613s server-side) |
| 2026-09-01 | FY2022, FY2023, FY2024 | canary passed 22:45:29Z (47,060 rows). Three full-year jobs submitted 180s apart. **Results filled in below when they land.** |
